import { createConfirmation } from "react-confirm";
import AddBillingFrequencyModal from "./AddBillingFrequencyModal";

export const showAddBillingFrequencyModal = createConfirmation(AddBillingFrequencyModal);
